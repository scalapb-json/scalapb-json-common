language: scala
dist: xenial
sudo: false
scala:
- 2.11.12
- 2.12.8
- 2.13.0
jdk: openjdk8
script:
- sbt "scalafmtCheck" "test:scalafmtCheck" "scalafmtSbtCheck" "++ ${TRAVIS_SCALA_VERSION}! -v" test
cache:
  directories:
  - $HOME/.ivy2/cache
  - $HOME/.sbt/launchers
before_cache:
- find $HOME/.ivy2/cache -name "ivydata-*.properties" | xargs rm
- find $HOME/.ivy2/cache -name "*SNAPSHOT*" | xargs rm -rf
matrix:
  include:
  - scala: 2.12.8
    jdk: openjdk11
  - sudo: required
    env: SCALA_NATIVE
    before_install:
    - curl https://raw.githubusercontent.com/scala-native/scala-native/master/scripts/travis_setup.sh | bash -
    script:
    - sbt "scalafmtCheck" "test:scalafmtCheck" "scalafmtSbtCheck" coreNative/test testsNative/test
